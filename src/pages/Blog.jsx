import { Box } from "@chakra-ui/react";
import React from "react";
import { BlogContainer } from "../containers";

const Blog = () => {
  return (
    <Box>
      <BlogContainer />
    </Box>
  );
};

export default Blog;
